{
  "name": "shaken-not-stirred",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd server && npm run seed\" \"cd client && npm start\"",
    "seed": "cd server && npm run seed && npm start",
    "install": "cd server && npm i && cd ../client && npm i",
    "build": "cd client && npm run build",
    "render-build": "npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "jwt-decode": "^3.1.2",
    "react-router-dom": "^6.28.0"
  }
}
